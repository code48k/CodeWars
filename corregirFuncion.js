// Corregir la función

// Creé esta función para sumar cinco a cualquier número que se le pase y devolver el nuevo valor. No genera ningún error, pero devuelve un número incorrecto.

// ¿Pueden ayudarme a corregir la función?

let num = 22;

function sumar(suma){

    let resultado = num + suma;
    
    return resultado;
}

console.log(sumar(5));