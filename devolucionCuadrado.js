// Ahora tienes que escribir una función que tome un argumento y devuelva el cuadrado del mismo.

function devolucionCuadrado(numeroCuadrado){

    let resultadoCuadrado = numeroCuadrado ** 2;

    return resultadoCuadrado;
}

let resultado = devolucionCuadrado(5);

console.log(resultado);